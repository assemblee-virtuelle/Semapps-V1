{% extends "::admin.html.twig" %}

{% block title %}GrandsVoisinsBundle:Admin:show{% endblock %}

{% block pageContentTitle %}Mon profil{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('form/formInteractions.js') }}"></script>
    <script type='text/javascript'>
        $(document).ready(function () {
            $(".form-group").on('click', '.add-widget', function (event) {
                $model = $(this).parent().parent().find('.sf-value-block').first().find('.ref').first();
                console.log('Clonage du champ :');
                console.log($model);
                cloneWidget($model)
            });
        })
    </script>
{% endblock %}
{% block pageContent %}
    {{ parent() }}

    <div class="row">
        <div class="col col-sm-12">
            <ul id="searchTabs" class="nav nav-tabs">
                <li role="presentation" class="active pull-right">
                    <a class="profileEditSwitch" href="#">
                        <span class="glyphicon glyphicon-pencil"></span>&nbsp;Editer
                    </a>
                </li>
            </ul>
        </div>
    </div>
    <br>

    <div class="col-sm-3">
        <a href="#" class="thumbnail" class="profileEditSwitch">
            <img src="{{ asset('common/images/no_avatar.jpg') }}" style="width:100%"
                 alt="Picture">
        </a>
        <div class="profileEditToSwitch" style="display:none">
            <input type="file" class="form-control" id="changePicture"/>
        </div>
    </div>
    <form class="col-sm-9">
        <div class="profileEditToSwitch">
            <h2>Prénom Nom</h2>
            <ul class="list">
                <li><span>3 ans et demi</span></li>
                <li><span>Téléphone : 01 23 45 67 89</span></li>
                <li><span>Site web : <a href="http://www.site.com" target="_blank">http://www.site.com</a></span></li>
            </ul>
        </div>
    </form>
    <div class="profileEditToSwitch" style="display:none">
        {% if form is not null %}
            <form class="col-sm-9" action="{{ path('profileSave') }}" method="post"
                  id="form">
                <input name="url" value="{{ form.subject }}" type="hidden">
                <input name="uri" value="{{ form.subject }}" type="hidden">
                <input name="graphURI" value="{{ graphURI }}" type="hidden">
                {% set temp ='' %}
                {% for field in form.fields %}
                    <div class="form-group col col-lg-12">
                        <div class="col-lg-4">
                            {% if field.label != temp %}
                                <label for="{{ field.label }}"
                                       data-uri-property="{{ field.property }}">{{ field.label }}</label>
                            {% else %}
                                <label for="{{ field.label }}" data-uri-property="{{ field.property }}">--</label>
                            {% endif %}
                        </div>
                        <div class="col-lg-6 sf-value-block">

                            {% if( field.label|lower == 'type') %}
                                <input class="form-control" type="url"
                                       name="%3C{{ form.subject }}%3E+%3C{{ field.property }}%3E+%3C{{ field.value }}%3E+."
                                       value="{{ field.value }}">
                            {% else %}
                                {% if( field.widgetType|lower|split(' ')[0] == 'uri') %}
                                    <input class="form-control ref" type="url" name="{{ field.htmlName }}"
                                           value="{{ field.value }}">
                                {% else %}
                                    <input class="form-control ref" type="{{ field.widgetType|lower|split(' ')[0] }}"
                                           name={{ field.htmlName }} value="{{ field.value }}">
                                {% endif %}

                            {% endif %}
                        </div>
                        <div class="col-lg-2">
                            {% if field.label != temp %}
                                <button type="button" class="btn btn-primary add-widget" readonly="yes" size="1"
                                        title="Add another value for label">add line
                                </button>
                                {% set temp =field.label %}
                            {% endif %}
                        </div>
                    </div>
                {% endfor %}
                <input class="btn btn-primary-lgv pull-right" type="submit"
                       value="sauvegarder">
            </form>
        {% else %}
            <div class="alert alert-info">Une erreur s'est produite lors de l'affichage du formulaire</div>
        {% endif %}
    </div>
{% endblock %}

